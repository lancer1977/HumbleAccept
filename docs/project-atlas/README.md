# Project Atlas

`HumbleAccept` is a browser-extension utility repo with Humble and Steam automation scripts plus Node smoke tests.

## Validation

Run:

```bash
bash scripts/validate.sh
```

The gate runs the Steam smoke-test suite.
